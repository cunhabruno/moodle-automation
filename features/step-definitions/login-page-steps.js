import {Given, Then, When, Helpers, pageObjectsParser} from 'selenium-cucumber-easy';

Then('I log in ulbra moodle with the user {string} and password {string}', function (user, password) {
    //Helpers.writeText(pageObjectsParser.get2LevelsLocator())
});
